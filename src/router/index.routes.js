import { Router } from "express";
/* aqui importar los archivos de rutas */
// import productRouter from './products.routes.js';

const indexRouter = Router();

/* aqui configurar las rutas, ej: */

// indexRouter.use('/v1/api/sessions', sessionRouter);
// indexRouter.use('/v1/api/users', userRouter);
// indexRouter.use('/v1/api/products', productRouter);
// indexRouter.use('/v1/api/carts', cartRouter);
// indexRouter.use('/v1/api/payments', paymentRouter);

export default indexRouter;